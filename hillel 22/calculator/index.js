let plus = require("./plus")
let minus = require("./minus")
let del = require("./del")
let mult = require("./mult")
let set = require("./set")

module.exports = {
    result: 0,
    plus,
    minus,
    del,
    mult,
    set,
}